nelmio_api_doc:
    documentation:
        info:
            title: GitHub Repositories API
            description: API do odkrywania popularnych repozytoriów na GitHubie.
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
    # routes:
    #     path: /api/doc
    #     controller: Nelmio\ApiDocBundle\Controller\SwaggerUiAction
